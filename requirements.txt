streamlit  
opencv-python-headless  # Use headless version to avoid libGL issues  
pandas  
numpy  
torch  # If you need PyTorch, ensure it's compatible with CPU  
transformers  
ultralytics  # Ensure this is compatible with your use case  
Pillow
